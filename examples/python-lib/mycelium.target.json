{
  "name": "example-python-lib-dedupe",
  "kind": "python",
  "test_command": "python -c \"from dedupe import dedupe; assert dedupe([3, 1, 2, 1]) == [3, 1, 2]\"",
  "benchmark_command": "python benchmark.py",
  "metrics_parser": "json_stdout",
  "metric_name": "seconds",
  "lower_is_better": true,
  "warmup": 1,
  "repeats": 5,
  "manifest_version": "1.0",
  "variants": [
    {"name": "set-fastpath", "mode": "patch",
     "files": {"dedupe.py": "variants/dedupe_set.py"},
     "description": "O(n) seen-set instead of O(n^2) list scan"}
  ]
}
